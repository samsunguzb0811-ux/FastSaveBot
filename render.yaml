services:
  - type: worker
    name: fastsavebot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python downloader_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
